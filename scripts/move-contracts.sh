#!/usr/bin/env bash
# ! Warning ! This script is only useful if your contacts and stemapp projects are in the same folder,
#   and when executed from the contracts folder. You can use it or not, so deal with it (⌐■_■)
rm -rf ../stemapp/compiled-contracts
mv compiled-contracts ../stemapp/
